extends base.pug

block content
    h4 Change Title of video
    form(method="POST")
        input(name = "title",laceholder="Video Title",value=video.title,requried)
        input(value="Save",type="submit")